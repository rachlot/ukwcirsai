/**
 * placeholder TODO for widgets / form elements that still need to be implemented
 */
function Todo({ data }: { data: any }) {
    return <pre>{JSON.stringify(data, null, 2)}</pre>
}

export default Todo